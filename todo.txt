- Typechecked return values
  Idea: extender los calls with an extra parameter where the result is saved:
        Return<Position> p1;
        new Entrar(new String("volvo"),1,p1).o(new PosResult(false,1,carriles));
        or maybe new Entrar(new String("volvo"),1).o(new PosResult(false,1,carriles)).r(p1)
        also let calls be generic on the return type, and thus matched only with the corresponding
        oracles. If we don't specify the return we use a normal one.
        Thus we have a generic map, and also a specific, well-typed return mechanism.
- Continue documenting, e.g., document alternatives properly.

